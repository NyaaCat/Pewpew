From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: Codex <codex@local>
Date: Tue, 20 Jan 2026 02:52:19 +0000
Subject: [PATCH] Fix async pathing compat for 1.21.11


diff --git a/net/minecraft/world/entity/ai/navigation/PathNavigation.java b/net/minecraft/world/entity/ai/navigation/PathNavigation.java
index 8de22cf2036ea43ccf2254e87ed0e98fbbded0cd..db2c5a8f87e068c147926b691c0306d3a19b3966 100644
--- a/net/minecraft/world/entity/ai/navigation/PathNavigation.java
+++ b/net/minecraft/world/entity/ai/navigation/PathNavigation.java
@@ -296,9 +296,10 @@ public abstract class PathNavigation {
     }
 
     public void applyAsyncPath(final AsyncPath asyncPath, final Path path, final int reachRange) {
-        if (this.path != asyncPath) {
+        if (this.path != asyncPath.pendingPath()) {
             return;
         }
+        this.path = path;
         this.targetPos = path.getTarget();
         this.reachRange = reachRange;
         this.resetStuckTimeout();
diff --git a/net/minecraft/world/level/pathfinder/AsyncPath.java b/net/minecraft/world/level/pathfinder/AsyncPath.java
index fd30f658034d91f0623988bbafd1bceaee60160a..76beb0c901a5947939e8f3887c9333a321c89434 100644
--- a/net/minecraft/world/level/pathfinder/AsyncPath.java
+++ b/net/minecraft/world/level/pathfinder/AsyncPath.java
@@ -1,26 +1,31 @@
 package net.minecraft.world.level.pathfinder;
 
-import java.util.Collections;
 import java.util.List;
+import java.util.concurrent.ConcurrentHashMap;
 import javax.annotation.Nullable;
 import net.minecraft.core.BlockPos;
-import net.minecraft.world.entity.Entity;
-import net.minecraft.world.phys.Vec3;
 
-public final class AsyncPath extends Path {
+public final class AsyncPath {
+    private static final ConcurrentHashMap<Path, AsyncPath> PENDING = new ConcurrentHashMap<>();
     private final long requestId;
+    private final Path pendingPath;
     @Nullable
     private volatile Path resolved;
 
     public AsyncPath(final long requestId, final BlockPos target) {
-        super(Collections.emptyList(), target, false);
         this.requestId = requestId;
+        this.pendingPath = new Path(List.of(), target, false);
+        PENDING.put(this.pendingPath, this);
     }
 
     public long requestId() {
         return this.requestId;
     }
 
+    public Path pendingPath() {
+        return this.pendingPath;
+    }
+
     public boolean isReady() {
         return this.resolved != null;
     }
@@ -31,173 +36,11 @@ public final class AsyncPath extends Path {
     }
 
     void complete(@Nullable final Path path) {
-        this.resolved = path == null ? new Path(List.of(), this.getTarget(), false) : path;
+        this.resolved = path == null ? new Path(List.of(), this.pendingPath.getTarget(), false) : path;
+        PENDING.remove(this.pendingPath);
     }
 
     public static boolean isPending(@Nullable final Path path) {
-        return path instanceof AsyncPath asyncPath && !asyncPath.isReady();
-    }
-
-    @Override
-    public void advance() {
-        Path path = this.resolved;
-        if (path != null) {
-            path.advance();
-        }
-    }
-
-    @Override
-    public boolean notStarted() {
-        Path path = this.resolved;
-        return path == null || path.notStarted();
-    }
-
-    @Override
-    public boolean isDone() {
-        Path path = this.resolved;
-        return path != null && path.isDone();
-    }
-
-    @Nullable
-    @Override
-    public Node getEndNode() {
-        Path path = this.resolved;
-        return path == null ? null : path.getEndNode();
-    }
-
-    @Override
-    public Node getNode(int index) {
-        Path path = this.resolved;
-        if (path == null) {
-            return new Node(this.getTarget().getX(), this.getTarget().getY(), this.getTarget().getZ());
-        }
-        return path.getNode(index);
-    }
-
-    @Override
-    public void truncateNodes(int length) {
-        Path path = this.resolved;
-        if (path != null) {
-            path.truncateNodes(length);
-        }
-    }
-
-    @Override
-    public void replaceNode(int index, Node point) {
-        Path path = this.resolved;
-        if (path != null) {
-            path.replaceNode(index, point);
-        }
-    }
-
-    @Override
-    public int getNodeCount() {
-        Path path = this.resolved;
-        return path == null ? 0 : path.getNodeCount();
-    }
-
-    @Override
-    public int getNextNodeIndex() {
-        Path path = this.resolved;
-        return path == null ? 0 : path.getNextNodeIndex();
-    }
-
-    @Override
-    public void setNextNodeIndex(int currentPathIndex) {
-        Path path = this.resolved;
-        if (path != null) {
-            path.setNextNodeIndex(currentPathIndex);
-        }
-    }
-
-    @Override
-    public Vec3 getEntityPosAtNode(Entity entity, int index) {
-        Path path = this.resolved;
-        return path == null ? entity.position() : path.getEntityPosAtNode(entity, index);
-    }
-
-    @Override
-    public BlockPos getNodePos(int index) {
-        Path path = this.resolved;
-        return path == null ? this.getTarget() : path.getNodePos(index);
-    }
-
-    @Override
-    public Vec3 getNextEntityPos(Entity entity) {
-        Path path = this.resolved;
-        return path == null ? entity.position() : path.getNextEntityPos(entity);
-    }
-
-    @Override
-    public BlockPos getNextNodePos() {
-        Path path = this.resolved;
-        return path == null ? this.getTarget() : path.getNextNodePos();
-    }
-
-    @Override
-    public Node getNextNode() {
-        Path path = this.resolved;
-        if (path == null) {
-            return new Node(this.getTarget().getX(), this.getTarget().getY(), this.getTarget().getZ());
-        }
-        return path.getNextNode();
-    }
-
-    @Nullable
-    @Override
-    public Node getPreviousNode() {
-        Path path = this.resolved;
-        return path == null ? null : path.getPreviousNode();
-    }
-
-    @Override
-    public boolean sameAs(@Nullable Path other) {
-        Path path = this.resolved;
-        if (path == null) {
-            return false;
-        }
-        return path.sameAs(other);
-    }
-
-    @Override
-    public boolean canReach() {
-        Path path = this.resolved;
-        return path != null && path.canReach();
-    }
-
-    @Nullable
-    @Override
-    public Path.DebugData debugData() {
-        Path path = this.resolved;
-        return path == null ? null : path.debugData();
-    }
-
-    @Override
-    public void writeToStream(net.minecraft.network.FriendlyByteBuf buffer) {
-        Path path = this.resolved;
-        if (path != null) {
-            path.writeToStream(buffer);
-        }
-    }
-
-    @Override
-    public BlockPos getTarget() {
-        Path path = this.resolved;
-        return path == null ? super.getTarget() : path.getTarget();
-    }
-
-    @Override
-    public float getDistToTarget() {
-        Path path = this.resolved;
-        return path == null ? Float.MAX_VALUE : path.getDistToTarget();
-    }
-
-    @Override
-    public Path copy() {
-        Path path = this.resolved;
-        if (path == null) {
-            return new Path(List.of(), this.getTarget(), false);
-        }
-        return path.copy();
+        return path != null && PENDING.containsKey(path);
     }
-}
\ No newline at end of file
+}
diff --git a/net/minecraft/world/level/pathfinder/AsyncPathService.java b/net/minecraft/world/level/pathfinder/AsyncPathService.java
index 59f07a462120fb6c3f1ee9c556afcfadb634c91d..549850e039ce0ab8b20006db58be58e1a038a58b 100644
--- a/net/minecraft/world/level/pathfinder/AsyncPathService.java
+++ b/net/minecraft/world/level/pathfinder/AsyncPathService.java
@@ -118,7 +118,7 @@ public final class AsyncPathService {
         final UUID mobId = mob.getUUID();
         PendingRequest existing = this.pending.get(mobId);
         if (existing != null) {
-            return existing.asyncPath;
+            return existing.asyncPath.pendingPath();
         }
         init();
         final ThreadPoolExecutor executor = this.executor;
@@ -153,7 +153,7 @@ public final class AsyncPathService {
         );
         if (this.pending.putIfAbsent(mobId, request) != null) {
             PendingRequest current = this.pending.get(mobId);
-            return current == null ? null : current.asyncPath;
+            return current == null ? null : current.asyncPath.pendingPath();
         }
         try {
             executor.execute(request::runAsync);
@@ -161,7 +161,7 @@ public final class AsyncPathService {
             this.pending.remove(mobId, request);
             return null;
         }
-        return asyncPath;
+        return asyncPath.pendingPath();
     }
 
     private boolean schedulePoiInternal(
@@ -354,12 +354,15 @@ public final class AsyncPathService {
             }
             final Entity entity = this.level.getEntity(this.mobId);
             if (!(entity instanceof Mob mob) || !mob.isAlive()) {
+                this.asyncPath.complete(null);
                 return;
             }
             if (mob.getNavigation() != this.navigation) {
+                this.asyncPath.complete(null);
                 return;
             }
-            if (this.navigation.getPath() != this.asyncPath) {
+            if (this.navigation.getPath() != this.asyncPath.pendingPath()) {
+                this.asyncPath.complete(null);
                 return;
             }
             if (!mob.blockPosition().closerThan(this.originPos, 8.0)) {
@@ -376,7 +379,7 @@ public final class AsyncPathService {
 
         private void completeFailure() {
             this.asyncPath.complete(null);
-            if (this.navigation.getPath() == this.asyncPath) {
+            if (this.navigation.getPath() == this.asyncPath.pendingPath()) {
                 this.navigation.stop();
             }
         }
@@ -485,4 +488,4 @@ public final class AsyncPathService {
             this.callback.accept(path);
         }
     }
-}
\ No newline at end of file
+}
diff --git a/net/minecraft/world/level/pathfinder/SnapshotPathNavigationRegion.java b/net/minecraft/world/level/pathfinder/SnapshotPathNavigationRegion.java
index 1107234b841ad52d94d016afe63396f5b6a96afc..bc6e8446c7383c34d2089262f80db84ed8e6cfe7 100644
--- a/net/minecraft/world/level/pathfinder/SnapshotPathNavigationRegion.java
+++ b/net/minecraft/world/level/pathfinder/SnapshotPathNavigationRegion.java
@@ -118,8 +118,7 @@ public final class SnapshotPathNavigationRegion implements CollisionGetter {
             }
         }
 
-        WorldBorder border = new WorldBorder();
-        border.applySettings(level.getWorldBorder().createSettings());
+        WorldBorder border = new WorldBorder(new WorldBorder.Settings(level.getWorldBorder()));
         return new SnapshotPathNavigationRegion(
             minChunkX,
             minChunkZ,
