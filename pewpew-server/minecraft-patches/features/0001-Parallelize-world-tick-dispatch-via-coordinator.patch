From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: Codex <codex@local>
Date: Sat, 17 Jan 2026 17:48:48 +0000
Subject: [PATCH] Parallelize world tick dispatch via coordinator


diff --git a/net/minecraft/server/MinecraftServer.java b/net/minecraft/server/MinecraftServer.java
index 9243bb11e3f968d0bf0eb2e3dc9295c0232bc15d..d3bfee1b6d6da25a1e976c04ce5b3ab9b7a9370b 100644
--- a/net/minecraft/server/MinecraftServer.java
+++ b/net/minecraft/server/MinecraftServer.java
@@ -1705,32 +1705,79 @@ public abstract class MinecraftServer extends ReentrantBlockableEventLoop<TickTa
         }
 
         this.isIteratingOverLevels = true; // Paper - Throw exception on world create while being ticked
-        for (ServerLevel serverLevel : this.getAllLevels()) {
-            serverLevel.hasPhysicsEvent = org.bukkit.event.block.BlockPhysicsEvent.getHandlerList().getRegisteredListeners().length > 0; // Paper - BlockPhysicsEvent
-            serverLevel.hasEntityMoveEvent = io.papermc.paper.event.entity.EntityMoveEvent.getHandlerList().getRegisteredListeners().length > 0; // Paper - Add EntityMoveEvent
-            serverLevel.updateLagCompensationTick(); // Paper - lag compensation
-            net.minecraft.world.level.block.entity.HopperBlockEntity.skipHopperEvents = serverLevel.paperConfig().hopper.disableMoveEvent || org.bukkit.event.inventory.InventoryMoveItemEvent.getHandlerList().getRegisteredListeners().length == 0; // Paper - Perf: Optimize Hoppers
-            profilerFiller.push(() -> serverLevel + " " + serverLevel.dimension().location());
-            /* Drop global time updates
-            if (this.tickCount % 20 == 0) {
-                profilerFiller.push("timeSync");
-                this.synchronizeTime(serverLevel);
-                profilerFiller.pop();
-            }
-            // CraftBukkit end */
+        if (io.papermc.paper.pewpew.WorldTickCoordinator.isEnabled()) {
+            Map<String, Runnable> worldTasks = Maps.newLinkedHashMap();
+            for (ServerLevel serverLevel : this.getAllLevels()) {
+                final String worldKey = serverLevel.dimension().location().toString();
+                worldTasks.put(worldKey, () -> {
+                    ProfilerFiller worldProfiler = Profiler.get();
+                    serverLevel.hasPhysicsEvent = org.bukkit.event.block.BlockPhysicsEvent.getHandlerList().getRegisteredListeners().length > 0; // Paper - BlockPhysicsEvent
+                    serverLevel.hasEntityMoveEvent = io.papermc.paper.event.entity.EntityMoveEvent.getHandlerList().getRegisteredListeners().length > 0; // Paper - Add EntityMoveEvent
+                    serverLevel.updateLagCompensationTick(); // Paper - lag compensation
+                    net.minecraft.world.level.block.entity.HopperBlockEntity.skipHopperEvents = serverLevel.paperConfig().hopper.disableMoveEvent || org.bukkit.event.inventory.InventoryMoveItemEvent.getHandlerList().getRegisteredListeners().length == 0; // Paper - Perf: Optimize Hoppers
+                    worldProfiler.push(() -> serverLevel + " " + serverLevel.dimension().location());
+                    /* Drop global time updates
+                    if (this.tickCount % 20 == 0) {
+                        worldProfiler.push("timeSync");
+                        this.synchronizeTime(serverLevel);
+                        worldProfiler.pop();
+                    }
+                    // CraftBukkit end */
 
-            profilerFiller.push("tick");
+                    io.papermc.paper.pewpew.WorldTickCoordinator.get().drainCommits(List.of(serverLevel.getWorld().getName()));
+                    worldProfiler.push("tick");
 
+                    try {
+                        serverLevel.tick(hasTimeLeft);
+                    } catch (Throwable var7) {
+                        CrashReport crashReport = CrashReport.forThrowable(var7, "Exception ticking world");
+                        serverLevel.fillReportDetails(crashReport);
+                        throw new ReportedException(crashReport);
+                    }
+
+                    worldProfiler.pop();
+                    worldProfiler.pop();
+                    serverLevel.explosionDensityCache.clear(); // Paper - Optimize explosions
+                });
+            }
+            io.papermc.paper.pewpew.WorldTickCoordinator.TickBarrier barrier =
+                io.papermc.paper.pewpew.WorldTickCoordinator.get().dispatchWorldTicks(worldTasks);
             try {
-                serverLevel.tick(hasTimeLeft);
-            } catch (Throwable var7) {
-                CrashReport crashReport = CrashReport.forThrowable(var7, "Exception ticking world");
-                serverLevel.fillReportDetails(crashReport);
-                throw new ReportedException(crashReport);
+                barrier.await();
+            } catch (InterruptedException interrupted) {
+                Thread.currentThread().interrupt();
+                throw new RuntimeException("Interrupted while waiting for world ticks", interrupted);
             }
+        } else {
+            for (ServerLevel serverLevel : this.getAllLevels()) {
+                serverLevel.hasPhysicsEvent = org.bukkit.event.block.BlockPhysicsEvent.getHandlerList().getRegisteredListeners().length > 0; // Paper - BlockPhysicsEvent
+                serverLevel.hasEntityMoveEvent = io.papermc.paper.event.entity.EntityMoveEvent.getHandlerList().getRegisteredListeners().length > 0; // Paper - Add EntityMoveEvent
+                serverLevel.updateLagCompensationTick(); // Paper - lag compensation
+                net.minecraft.world.level.block.entity.HopperBlockEntity.skipHopperEvents = serverLevel.paperConfig().hopper.disableMoveEvent || org.bukkit.event.inventory.InventoryMoveItemEvent.getHandlerList().getRegisteredListeners().length == 0; // Paper - Perf: Optimize Hoppers
+                profilerFiller.push(() -> serverLevel + " " + serverLevel.dimension().location());
+                /* Drop global time updates
+                if (this.tickCount % 20 == 0) {
+                    profilerFiller.push("timeSync");
+                    this.synchronizeTime(serverLevel);
+                    profilerFiller.pop();
+                }
+                // CraftBukkit end */
+
+                io.papermc.paper.pewpew.WorldTickCoordinator.get().drainCommits(List.of(serverLevel.getWorld().getName()));
+                profilerFiller.push("tick");
+
+                try {
+                    serverLevel.tick(hasTimeLeft);
+                } catch (Throwable var7) {
+                    CrashReport crashReport = CrashReport.forThrowable(var7, "Exception ticking world");
+                    serverLevel.fillReportDetails(crashReport);
+                    throw new ReportedException(crashReport);
+                }
 
-            profilerFiller.pop();
-            profilerFiller.pop();
-            serverLevel.explosionDensityCache.clear(); // Paper - Optimize explosions
+                profilerFiller.pop();
+                profilerFiller.pop();
+                serverLevel.explosionDensityCache.clear(); // Paper - Optimize explosions
+            }
         }
+        io.papermc.paper.pewpew.WorldTickCoordinator.get().drainGlobalCommits();
         this.isIteratingOverLevels = false; // Paper - Throw exception on world create while being ticked
